#include <stdio.h>

/**
 * main - this is the main function that prints exactly what we want
 *
 * Return: 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
